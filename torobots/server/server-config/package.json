{
	"private": true,
	"version": "0.1.0",
	"name": "@torobot/server-config",
	"scripts": {
		"build":"tsc --build",
		"start": "yarn build && node dist/index.js"
	},
	"dependencies": {
		"shelljs": "^0.8.4"
	}
}
