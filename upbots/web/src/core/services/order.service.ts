import { Strategy } from "@/store/orders/types/types";

// export const splitStrategiesUp = (arr: Strategy[]): { long: Strategy[]; short: Strategy[] } => {
//   const long: Strategy[] = [];
//   const short: Strategy[] = [];

//   arr.forEach((s) => {
//     s.sideDescription === "long" ? long.push(s) : short.push(s);
//   });

//   return {
//     long,
//     short,
//   };
// };
