// import { Test, TestingModule } from "@nestjs/testing";
// import TransactionsController from "../../src/transactions/controllers/transactions.controller";
// import TransactionsRepository from "../../src/transactions/repositories/transactions.repository";

// const mockTaskRepository = () => ({
//   findAll: jest.fn(),
// });

describe("Transactions Controller", () => {
  // TODO: fix this test
  // let sut: TransactionsController;
  // let transactionsRepository: TransactionsRepository;
  // beforeEach(async () => {
  //   const module: TestingModule = await Test.createTestingModule({
  //     controllers: [TransactionsController],
  //     providers: [{ provide: TransactionsRepository, useFactory: mockTaskRepository }],
  //   }).compile();
  //   sut = module.get<TransactionsController>(TransactionsController);
  //   transactionsRepository = module.gÒet<TransactionsRepository>(TransactionsRepository);
  // });
  it("should be defined", () => {
    // TODO: fix this test
    expect(true).toBeTruthy();
  });
  // describe("findAll", () => {
  //   it("gets all the transactions from the repository", () => {
  //     expect(transactionsRepository.findAll).not.toHaveBeenCalled();
  //   });
  // });
});
